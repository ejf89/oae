<!-- list-collections.liquid -->

<div class="list-collections-container gutter">

  <h1 class="list-collections-header">{{ page_title }}</h1>

  <div class="grid-3">
  {% for collection in collections %}
    {% unless collection.handle == 'frontpage' %}
    <div class="list-collection-item">
      <a href="{{ collection.url }}" title="{{ 'collections.general.link_title' | t: title: title }}">
        {% if collection.image != blank %}
          {{ collection | img_url: '480x480' | img_tag: collection.title }}
        {% elsif collection.products.first != blank %}
          {{ collection.products.first | img_url: '480x480' | img_tag: collection.title }}
        {% else %}
          {% capture current %}{% cycle 1, 2, 3, 4, 5, 6 %}{% endcapture %}
          {{ 'collection-' | append: current | placeholder_svg_tag: 'placeholder-svg placeholder-svg--small' }}
        {% endif %}
      </a>
      <p><a class="list-collection-item_title" href="{{ collection.url }}" title="{{ 'collections.general.link_title' | t: title: title }}">{{ collection.title }}</a></p>
    </div>
    {% endunless %}
  {% endfor %}
  </div>

</div>
