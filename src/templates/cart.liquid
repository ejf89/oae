<!-- cart.liquid -->
{% if cart.item_count > 0 %}

  <h1 class="cart-title ">Your Cart</h1>


  <form action="/cart" method="post" novalidate>
    <table class="responsive-table cart-table">
      <thead class="small--hide">
        <tr>
          <th colspan="2">{{ 'cart.label.product' | t }}</th>
          <th>{{ 'cart.label.price' | t }}</th>
          <th>{{ 'cart.label.quantity' | t }}</th>
          <th>{{ 'cart.label.total' | t }}</th>
        </tr>
      </thead>
      <tbody>

        {% for item in cart.items %}

        {% comment %}

        Cart Item Template
        =====================
        The data-label attributes on <td> elements are mobile-friendly
        helpers used for responsive-table labels
        {% endcomment %}

          <tr class="responsive-table-row">

            <td class="cart-item" data-label="{{ 'customer.order.product' | t }}">

              {% if item.image != blank %}
                <a href="{{ item.url | within: collections.all }}">
                  {{ item | img_url: '200x200' | img_tag: item.title }}
                </a>
              {% endif %}

              <div class="content">

                <a class="text-left cart-item-title" href="{{ item.url }}">{{ item.product.title }}</a>

                <p>x {{item.quantity}}</p>

                {% if item.original_line_price != item.line_price %}
                  <span class="visually-hidden">{{ 'cart.label.discounted_price' | t }}</span>
                  {{ item.price | money }}
                  <span class="visually-hidden">{{ 'cart.label.original_price' | t }}</span>
                  <s>{{ item.original_price | money }}</s>
                {% else %}
                  {{ item.price | money }}
                {% endif %}
              </div>
            </td>
            <td>
              <!-- <a class="text-center" href="{{ item.url }}">{{ item.product.title }}</a> -->

              {% unless item.product.has_only_default_variant %}
                <p class="text-center">{{ item.variant.title}}</p>
              {% endunless %}

              <!-- <p class="text-center">{{ item.vendor }}</p> -->

              {%- assign property_size = item.properties | size -%}

              {% if property_size > 0 %}
                {% for p in item.properties %}
                  {% unless p.last == blank %}
                  {{ p.first }}:

                  {% if p.last contains '/uploads/' %}
                    <a href="{{ p.last }}">{{ p.last | split: '/' | last }}</a>
                  {% else %}
                    {{ p.last }}
                  {% endif %}

                  {% endunless %}
                {% endfor %}
              {% endif %}

              <!-- <a href="/cart/change?line={{ forloop.index }}&amp;quantity=0">
                <small>{{ 'cart.general.remove' | t }}</small>
              </a> -->
            </td>


          </tr>

        {% endfor %}
      </tbody>
    </table>

    <div class="space-m">

    </div>

    {% if settings.cart_notes_enable %}

    <div class="note-container">
      <label for="CartSpecialInstructions">{{ 'cart.general.note' | t }}:</label>
      <textarea name="note" id="CartSpecialInstructions">{{ cart.note }}</textarea>
    </div>
    {% endif %}


  <div class="checkout-info-container">
      <p>{{ 'cart.general.subtotal' | t }}</p>
      <p>{{ cart.total_price | money }}</p>
      {% if cart.total_discounts > 0 %}
        <p>{{ 'cart.general.savings' | t }} {{ cart.total_discounts | money }}</p>
      {% endif %}
      <p>{{ 'cart.general.shipping_at_checkout' | t }}</p>
      <input class="btn cta cta-blue" type="submit" name="update" value="{{ 'cart.general.update' | t }}">
      <input type="submit" name="checkout" class="btn cta cta-blue" value="{{ 'cart.general.checkout' | t }}">
    </div>

  </form>
{% else %}
  <h1>{{ 'cart.general.title' | t }}</h1>

  {% comment %}
  Cart empty state
  {% endcomment %}
  <div class="supports-cookies">
    <p>{{ 'cart.general.empty' | t }}</p>
    <p>{{ 'cart.general.continue_browsing_html' | t }}</p>
  </div>

  {% comment %}
  Cart no cookies state
  ---------------------
  Browser cookies are required to use the cart. If cookies aren't enabled in the
  browser a message is displayed prompting the user to enable them.
  {% endcomment %}
  <div class="supports-no-cookies">
    <p>{{ 'cart.general.cookies_required' | t }}</p>
  </div>
{% endif %}
